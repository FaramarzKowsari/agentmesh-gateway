# Portable build verification

This marker intentionally triggers the permanent cross-platform `portable-binaries` pull-request workflow so the Windows, Linux and macOS standalone artifacts can be built and smoke-tested before release publication.
