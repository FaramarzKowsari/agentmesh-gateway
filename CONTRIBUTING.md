# Contributing

Use Python 3.11 or newer. Create a focused branch, install `pip install -e '.[dev]'`, and run `ruff check .` plus `pytest` before submitting a pull request. Preserve the boundaries in `ARCHITECTURE.md`, add deterministic tests, update behavior documentation, and never commit credentials or copy material from other gateway repositories.
