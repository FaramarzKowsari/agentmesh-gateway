# Contributing

1. Read `PROVENANCE.md` and `AGENTS.md`.
2. Create a focused branch.
3. Add tests for behavior changes.
4. Run `ruff check .` and `pytest`.
5. Open a PR describing design, tests, compatibility and provenance impact.

Do not submit secrets or copied/unattributed code.
